services:
  - type: web
    name: it-hub-vn
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: it-hub-vn-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1